## What & why

<!-- What does this change and why. Link issues. -->

## Checklist

- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` and `pnpm lint:arch` pass
- [ ] `pnpm test` passes (added/updated tests for the change)
- [ ] Specs in `spec/` updated if behavior changed
